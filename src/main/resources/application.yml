server:
    port: 8080
spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/spring-boot-security-demo?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
        username: root
        password: 123456
        schema: classpath:schema.sql
        data: classpath:data.sql

mybatis-plus:
    global-config:
        db-config:
            id-type: auto
